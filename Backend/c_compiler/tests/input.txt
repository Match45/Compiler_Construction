int add(int a, int b) {
    return a + b;
}

int main() {
    int x = 5;
    int y = 10;
    int sum = add(x, y);
    if (sum > 10) {
        sum = sum - 1;
    }
    while (x < y) {
        x = x + 1;
    }
    return 0;
}